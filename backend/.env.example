DATABASE_URL="postgresql://postgres:12345678@localhost:5432/dokan_erp?schema=public"
# Required in production (>=16 chars). Dev falls back to an insecure default.
AUTH_JWT_SECRET="dev-only-auth-secret"
# Optional: comma-separated CORS allow-list for production, e.g.
# CORS_ALLOWED_ORIGINS="https://app.dokanerp.com,https://admin.dokanerp.com"
